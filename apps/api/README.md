# API application

Futura superficie HTTP del puente. Debe limitarse a autenticación, autorización, rate limits, parsing de entrada, idempotencia y orquestación hacia contratos/core. No contendrá reglas fiscales duplicadas.
